// This file is updated at build time by scripts/build.ts.
// In development, run `bun run build` to update the version.
export const APP_VERSION = "0.1.0"

export function getVersion(): string {
  return APP_VERSION
}
